# 2026-06-17

## 首页「号池 / 接码」新增 SMSBower 入口
- 文件：`src/components/InfoCollections.tsx`，`COLLECTIONS` → 「资源工具」group → `号池 / 接码` section。
- 在 Hero SMS 后追加 SMSBower（https://smsbower.app/），tag「短信接码 / SMS」，描述「虚拟号码接码平台，覆盖 Telegram、Google、WhatsApp 等服务」。
- 该 section 计数由 `section.items.length` 自动渲染，无需额外改动。
- 修改后需 `npm run build` 更新 dist 才能在扩展中生效。
